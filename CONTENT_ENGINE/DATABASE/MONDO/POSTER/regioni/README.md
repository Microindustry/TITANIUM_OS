# 💎 regioni — la collana delle 8 Regioni (card social)

**Sei in:** `POSTER/regioni/` — una card per Pietra ⟡0→⟡7 (1080×1350): colore-tema,
gemma, episodio-madre. È la LEGENDA VISIVA della mappa-gioco.

⚠️ **Cartella GENERATA**: le scrive `CONTENT_ENGINE/scripts/nina_region_posters.py`
(legge episodes.json e la palette in `../README.md`). Non editare i file a mano —
modifica lo script e rigenera:

```
python CONTENT_ENGINE/scripts/nina_region_posters.py
```

*(il nome resta minuscolo `regioni/` perché lo script scrive qui — non rinominare)*
