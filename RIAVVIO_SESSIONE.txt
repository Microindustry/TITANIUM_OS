=== RIAVVIO SESSIONE #31 — 06/06/2026 ===
Ultima sessione: #31 (autonoma, Matteo a dormire) — 2026-06-06

━━━ STAVAMO PARLANDO DI ━━━
Completato il lavoro interrotto della #30: la CHAT RAG VERA in dashboard (P4a)
+ la LEVA LLM LOCALE (P4b). Il RAG non e' piu' solo magazzino: ora si interroga
dalla dashboard (sotto-voce ciano "Chat RAG" sotto GENESIS) e risponde citando
le fonti reali di MENTE/. Con toggle motore Claude/Locale (Ollama+Qwen).

━━━ COSA HO TROVATO (sessione interrotta) ━━━
  • RagChatView.tsx + /api/rag/chat + /api/llm/ollama/status + ollama_client.py
    erano GIA' costruiti ma NON committati e NON testati (interrotto prima del commit).
  • Avevo aggiunto per errore un /api/rag/ask ridondante -> RIMOSSO (c'era gia' /chat).
  • tsc -b falliva per 6 errori di tipo PRE-ESISTENTI (non miei) -> sistemati.

━━━ FATTO QUESTA SESSIONE ━━━
  ✓ Commit 1bd09e5 (feat rag P4a/P4b): chat RAG + leva locale + build verde
  ✓ Commit 3258887 (chore): SERVICES/restart_api.ps1 (restart api_server auto-UAC)
  ✓ Testato /api/rag/chat via test_client: HTTP 200, engine_used=claude, fonti
    citate (v32_specifiche.md), risposta "178 kg corpo unico" corretta — REALE.
  ✓ Leva locale onesta: available=false -> fallback Claude con flag (no teatro)
  ✓ Spuntati PROSSIMA_SESSIONE.md + Desktop\TITANIUM_OS_PIANO.txt (P4a FATTO, P4b leva)

━━━ THREAD APERTI (da riprendere) ━━━
  → ACCENDERE LEVA LOCALE: installare Ollama + `ollama pull qwen2.5:7b-instruct-q4_K_M`
  → RESTART api_server per endpoint LIVE: doppio click SERVICES\restart_api.ps1
    (auto-UAC). Oppure va live da solo al prossimo login (watchdog AtLogon).
  → P1b: critiche LIVE in dashboard da critiche_auto.json (cartella clinica, Visione B)
  → P5: agenti visibili ("sto interrogando X") + verificare se sono giusti per noi
  → P3a: recuperare episodi orfani in episodes.json

━━━ MURO TECNICO INCONTRATO ━━━
  ⚠ api_server (PID elevato, spawnato dal watchdog RunLevel Highest) NON e'
    uccidibile da shell normale (Medium IL). Restart richiede UAC: per questo
    restart_api.ps1 si auto-eleva. Endpoint nuovi non live nell'istanza corrente
    finche' non si fa il restart (o login).

━━━ BLOCKERS ━━━
  ⚠ Mandrino 2.2kW ER20 da ordinare — prerequisito fresatura stampi MIMS
  ⚠ Silent blocks: decisione v.A (senza) vs v.B (18mm) — prerequisito V8

━━━ ULTIMI COMMIT ━━━
  3258887 chore(services): restart_api.ps1 — riavvio api_server con auto-UAC
  1bd09e5 feat(rag): Chat RAG vera in dashboard + leva LLM locale (P4a/P4b)
  64c7212 docs(p4b): ricerca LLM locale (Simone Rizzo) + decisione Qwen2.5-7B-Q4

Generato: 06/06/2026 (sessione autonoma)
